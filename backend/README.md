# Backend

A simple Express API that mimics a production service with health checks and status endpoints.

## Development

```bash
npm install
npm run dev
```

## Endpoints

- `GET /api/status`
- `GET /api/auth/health`
- `GET /api/jobs/health`
